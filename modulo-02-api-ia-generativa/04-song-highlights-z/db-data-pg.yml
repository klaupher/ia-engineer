services:
  postgres:
    container_name: postgres
    restart: always
    image: postgres
    ports:
      - 5433:5432
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_DB_PASS}
      POSTGRES_DB: song_recommender
    volumes:
        - "./dbdata:/var/lib/postgresql"

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
